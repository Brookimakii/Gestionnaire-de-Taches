<header>
        <nav class="navbar navbar-expand-lg navbar-light bg-light">
            <div class="container">
                <a class="navbar-brand" href="javascript:history.back()">
                    <img src="{{ asset('image/retour.png') }}" alt="Retour" class="d-inline-block align-top" width="30" height="30">
                </a>
                <a class="navbar-brand" href="#">
                    <img src="{{ asset('image/burger.png') }}" alt="Logo" class="d-inline-block align-top" width="30" height="30">
                </a>
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarSupportedContent">
                    <ul class="navbar-nav ml-auto">
                        {% if app.user %}
                            <li class="nav-item">
                               {# <a class="nav-link" href="{{ path('app_user_profile') }}">{{ app.user.username[0] }}</a> #}
                            </li>
                        {% endif %}
                    </ul>
                </div>
            </div>
        </nav>
    </header>