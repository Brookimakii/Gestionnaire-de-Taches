<header>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container d-flex justify-content-between align-items-center">
            <a class="navbar-brand" href="javascript:history.back()">
                <img src="{{ asset('image/retour.png') }}" alt="Retour" class="d-inline-block align-top" width="25" height="25">
            </a>
            <div class="logo-container text-center flex-grow-1">
                <a class="navbar-brand mx-auto" href="#">
                    <img src="{{ asset('image/task2.png') }}" alt="Logo" class="d-inline-block align-top" width="170" height="60">
                </a>
            </div>
            <div class="navbar-collapse collapse" id="navbarSupportedContent">
                <ul class="navbar-nav ml-auto">
                    {% if app.user %}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_user_profile') }}">
                                {% if app.user.pseudo %}
                                    {{ app.user.pseudo|first }}
                                {% else %}
                                    Utilisateur
                                {% endif %}
                            </a>
                        </li>
                    {% endif %}
                </ul>
            </div>
        </div>
    </nav>
</header>

<style>
    header {
        margin-top:10px;
        border-top: 1px solid lightgrey;
        border-bottom: 1px solid lightgrey;
    }
    .navbar {
        background-color: white !important;
    }
    .logo-container {
        position: relative;
        width: 100%;
        text-align: center;
    }
    .nav-link{
        font-size:30px;
    }
</style>
