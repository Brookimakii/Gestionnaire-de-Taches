<header>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container d-flex justify-content-between align-items-center">
            <a class="navbar-brand" href="javascript:history.back()">
                <img src="{{ asset('image/retour.png') }}" alt="Retour" class="d-inline-block align-top" width="25" height="25">
            </a>
            <div class="logo-container text-center flex-grow-1">
                <a class="navbar-brand mx-auto" href="#">
                    <img src="{{ asset('image/task2.png') }}" alt="Logo" class="d-inline-block align-top" width="170" height="60">
                </a>
            </div>
            <div class="navbar-collapse collapse" id="navbarSupportedContent">
                <ul class="navbar-nav ml-auto">
                    {% if app.user %}
                        <li class="nav-item">
                            <a  href="{{ path('app_profile') }}" class="profile-initials rounded-circle mb-3" style="width: 50px; height: 50px; background-color: #A3C4F3; color: #fff; display: flex; align-items: center; justify-content: center; font-size: 30px;">
                                {% if app.user.pseudo %}
                                    {{ app.user.pseudo|first }}
                                {% else %}
                                    Utilisateur
                                {% endif %}
                            </a>
                        </li>
                        <li class="nav-item">
                            {% if app.user %}
                            <div class="mb-3">
                                <a href="{{ path('app_logout') }}">
                                    <img src="{{ asset('image/se-deconnecter.png') }}" alt="se déconnecter" class="header-icon">
                                </a>
                            </div>
                            {% endif %}
                            </a>
                        </li>
                    {% endif %}
                </ul>
            </div>
        </div>
    </nav>
</header>

<style>
    header {
        margin-top:10px;
        border-top: 1px solid lightgrey;
        border-bottom: 1px solid lightgrey;
    }
    .navbar {
        background-color: white !important;
    }
    .logo-container {
        position: relative;
        width: 100%;
        text-align: center;
    }
    .nav-link{
        font-size:30px;
    }
    .header-icon{
        height: 30px;
        width: auto;
        margin-left: 5px;
        margin-top: 16px;
    }
</style>
